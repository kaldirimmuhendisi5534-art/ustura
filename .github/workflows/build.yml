name: Ustura Android Build

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build profili (preview=APK, production=AAB)'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production

permissions:
  contents: write

jobs:
  build:
    name: Android Build
    runs-on: self-hosted

    steps:
      - name: Setup Java and Android PATH
        shell: powershell
        run: |
          Add-Content $env:GITHUB_PATH "C:\Program Files\Git\bin"
          Add-Content $env:GITHUB_PATH "C:\Program Files\Git\usr\bin"
          $javaHome    = "$env:LOCALAPPDATA\Java\jdk-17.0.11+9"
          $androidHome = "$env:LOCALAPPDATA\Android\Sdk"
          Add-Content $env:GITHUB_ENV  "JAVA_HOME=$javaHome"
          Add-Content $env:GITHUB_ENV  "ANDROID_HOME=$androidHome"
          Add-Content $env:GITHUB_PATH "$javaHome\bin"
          Add-Content $env:GITHUB_PATH "$androidHome\platform-tools"
          Add-Content $env:GITHUB_PATH "$androidHome\cmdline-tools\latest\bin"
          Write-Host "JAVA_HOME=$javaHome"
          Write-Host "ANDROID_HOME=$androidHome"

      - name: Clone to short path C-slash-p
        shell: powershell
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if (Test-Path "C:\p") { Remove-Item -Recurse -Force "C:\p" }
          $repo = "${{ github.repository }}"
          $ref  = "${{ github.ref_name }}"
          git clone --depth 1 --branch $ref "https://x-access-token:$env:GITHUB_TOKEN@github.com/$repo.git" "C:\p"
          if ($LASTEXITCODE -ne 0) { throw "git clone failed" }
          Write-Host "Cloned to C:\p"
          Write-Host "package.json: $(Test-Path 'C:\p\package.json')"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        shell: bash
        run: cd /c/p && npm install --legacy-peer-deps

      - name: Expo Prebuild
        shell: bash
        run: cd /c/p && npx expo prebuild --platform android --non-interactive --clean

      - name: Gradle Build
        shell: bash
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
        run: |
          PROFILE="${{ github.event.inputs.profile }}"
          KEYSTORE_WIN=$(powershell.exe -Command "echo \$env:USERPROFILE" | tr -d '\r')
          KEYSTORE="$KEYSTORE_WIN/.android/debug.keystore"
          cd /c/p/android
          if [ "$PROFILE" = "production" ]; then
            ./gradlew bundleRelease --no-daemon \
              -Pandroid.injected.signing.store.file="$KEYSTORE" \
              -Pandroid.injected.signing.store.password=android \
              -Pandroid.injected.signing.key.alias=androiddebugkey \
              -Pandroid.injected.signing.key.password=android
            cp app/build/outputs/bundle/release/app-release.aab /c/p/ustura.aab
          else
            ./gradlew assembleRelease --no-daemon \
              -Pandroid.injected.signing.store.file="$KEYSTORE" \
              -Pandroid.injected.signing.store.password=android \
              -Pandroid.injected.signing.key.alias=androiddebugkey \
              -Pandroid.injected.signing.key.password=android
            cp app/build/outputs/apk/release/app-release.apk /c/p/ustura.apk
          fi
          ls -lh /c/p/ustura*

      - name: Create GitHub Release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="build-${{ github.run_number }}"
          gh release create "$TAG" /c/p/ustura* \
            --title "Ustura Build ${{ github.run_number }}" \
            --notes "Ustura APK - Build ${{ github.run_number }} - Branch ${{ github.ref_name }}" \
            --prerelease \
            --repo ${{ github.repository }}
